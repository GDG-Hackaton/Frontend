# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Hero Full-Screen and Navbar Overlay
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bugs, scope the property to the concrete failing case(s) to ensure reproducibility
  - Test that when the landing page is rendered:
    - Hero section starts at 0px from viewport top (currently fails - starts at 64px/80px)
    - Navbar uses absolute positioning (currently fails - uses fixed)
    - Spacer div does not exist (currently fails - spacer div exists)
    - Navbar buttons have transparent backgrounds on landing page (currently fails - solid backgrounds)
    - Hero section is full viewport height (currently fails - 80vh)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - Hero section top offset is 64px (mobile) or 80px (desktop) instead of 0px
    - Navbar className contains "fixed" instead of "absolute"
    - Spacer div `<div className="h-16 md:h-20" />` exists at end of MainHeader
    - Navbar buttons have default styling instead of transparent backgrounds
    - Hero section uses `min-h-[80vh]` instead of `min-h-screen`
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.4, 2.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Navbar Functionality
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (navbar functionality on other pages and interactions)
  - Write property-based tests capturing observed behavior patterns:
    - Navbar scroll effects (backdrop blur when scrolled) work correctly
    - Mobile menu toggle and slide-out menu function correctly
    - Dropdown menus (Reconnect, Language, Profile) open and close correctly
    - Authentication state display (Sign in/Join vs Profile/Logout) works correctly
    - Navigation links work and show active states correctly
    - Logo click navigation to home page works correctly
    - Hover and click feedback on buttons provides visual feedback
    - Navigation on other pages (Cases, Report, Volunteers, Admin, Wanted) works correctly
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8_

- [x] 3. Fix for hero navbar overlay styling issues

  - [x] 3.1 Update MainHeader.jsx navbar positioning and styling
    - Change navbar positioning from `fixed` to `absolute` in className
    - Current: `className="fixed left-0 right-0 top-0 z-50"`
    - Fixed: `className="absolute left-0 right-0 top-0 z-50"`
    - Remove spacer div at end of MainHeader component
    - Current: `<div className="h-16 md:h-20" />`
    - Fixed: Remove this line entirely
    - Add conditional styling for landing page detection
    - Use `useLocation` hook to detect landing page: `const isLandingPage = location.pathname === '/';`
    - Update navbar background styling to be transparent on landing page when not scrolled
    - Current: Always applies backdrop blur when scrolled
    - Fixed: `className={`absolute left-0 right-0 top-0 z-50 transition-all duration-300 ${isScrolled ? "border-b border-stone-200 shadow-sm backdrop-blur-xl bg-white/80" : "bg-transparent"}`}`
    - Update navbar button styling for hero integration
    - Add conditional button classes: `const heroButtonClass = isLandingPage && !isScrolled ? 'bg-transparent text-white border-transparent hover:bg-white/10' : 'text-stone-700 hover:bg-stone-100 hover:text-charcoal';`
    - Apply heroButtonClass to primary navigation links
    - Update logo and icon colors for landing page
    - Add conditional text color: `const heroTextClass = isLandingPage && !isScrolled ? 'text-white' : 'text-charcoal';`
    - Apply heroTextClass to logo text, menu icons, dropdown icons
    - _Bug_Condition: isBugCondition(input) where input.page == "Landing" AND input.navbar.position == "fixed" AND input.navbar.hasSpacerDiv == true AND input.hero.topOffset > 0 AND input.navbar.buttons.background != "transparent"_
    - _Expected_Behavior: Hero section starts at 0px from viewport top, navbar uses absolute positioning, spacer div removed, navbar buttons have transparent backgrounds and white text on landing page_
    - _Preservation: All navbar functionality (scroll effects, mobile menu, dropdowns, authentication states, navigation, visual feedback) must remain unchanged_
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8_

  - [x] 3.2 Update Landing.jsx hero section height and padding
    - Change hero section height from 80vh to full viewport height
    - Current: `className="relative min-h-[80vh] flex items-center justify-center text-white overflow-hidden"`
    - Fixed: `className="relative min-h-screen flex items-center justify-center text-white overflow-hidden pt-16 md:pt-20"`
    - Add top padding to account for navbar overlay (pt-16 md:pt-20)
    - This ensures hero content isn't hidden behind the navbar
    - _Bug_Condition: isBugCondition(input) where input.hero.height == "80vh" AND input.hero.topPadding == 0_
    - _Expected_Behavior: Hero section is full viewport height (100vh) and has top padding for navbar overlay_
    - _Preservation: Hero section content and video background must remain unchanged_
    - _Requirements: 2.1, 2.5_

  - [x] 3.3 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Hero Full-Screen and Navbar Overlay
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify:
      - Hero section starts at 0px from viewport top
      - Navbar uses absolute positioning
      - Spacer div does not exist
      - Navbar buttons have transparent backgrounds and white text on landing page
      - Hero section is full viewport height (100vh)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.4 Verify preservation tests still pass
    - **Property 2: Preservation** - Navbar Functionality
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Verify all navbar functionality still works:
      - Navbar scroll effects (backdrop blur when scrolled)
      - Mobile menu toggle and slide-out menu
      - Dropdown menus (Reconnect, Language, Profile)
      - Authentication state display
      - Navigation links and active states
      - Logo click navigation
      - Hover and click feedback
      - Navigation on other pages
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all tests (bug condition exploration test + preservation tests)
  - Verify hero section is full-screen with navbar overlay on landing page
  - Verify navbar functionality is preserved on all pages
  - Verify no visual regressions on mobile and desktop viewports
  - Test scroll behavior from hero section to other sections
  - Test navigation from landing page to other pages and back
  - If any issues arise, ask the user for guidance before proceeding
